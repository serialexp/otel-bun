# TODO

## Completed Instrumentations

1. ~~**`Bun.redis`**~~ — Done. `instrumentRedis(client)` / `uninstrumentRedis(client)`.
2. ~~**`bun:sqlite`**~~ — Done. `instrumentSQLite(db)` / `uninstrumentSQLite(db)`.
3. ~~**`Bun.spawn`**~~ — Done. `instrumentSpawn()` / `uninstrumentSpawn()`.
4. ~~**WebSockets**~~ — Done. `instrumentWebSocket(handlers)`.
5. ~~**`Bun.sql` (PostgreSQL)**~~ — Done. `instrumentSQL(client)`.
